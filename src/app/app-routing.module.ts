import { NgModule } from '@angular/core';
import { PreloadAllModules, RouterModule, Routes } from '@angular/router';

const routes: Routes = [
  { path: '', redirectTo: 'home', pathMatch: 'full' },
  { path: 'home', loadChildren: () => import('./home/home.module').then( m => m.HomePageModule)},
  { path: 'pokemon-type-detail', loadChildren: './pokemon-type-detail/pokemon-type-detail.module#PokemonTypeDetailPageModule' },
  { path: 'pokemon-detail', loadChildren: './pokemon-detail/pokemon-detail.module#PokemonDetailPageModule' },
];

@NgModule({
  imports: [
    RouterModule.forRoot(routes, { preloadingStrategy: PreloadAllModules })
  ],
  exports: [RouterModule]
})
export class AppRoutingModule { }
